import XCTest
@testable import BleRxLayerModule

final class BleRxLayerModuleTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(BleRxLayerModule().text, "Hello, World!")
    }
}
